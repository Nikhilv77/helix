export default function Loading() {
  // Keep the persistent workspace chrome and canvas visible while the dynamic
  // report data resolves. A report-shaped fallback flashes too briefly on a
  // warm refresh and reads as a graphite card appearing behind the avatar.
  return null;
}
